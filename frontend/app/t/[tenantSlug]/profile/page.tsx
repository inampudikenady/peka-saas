"use client";
import{useParams}from"next/navigation";import{useState}from"react";import{Alert}from"@/components/alert";import{FormField}from"@/components/form-field";import{TenantShell}from"@/components/tenant-shell";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{useTenantUser}from"@/hooks/use-tenant-user";import{tenantApi}from"@/lib/api";
export default function TenantProfile(){const{tenantSlug}=useParams<{tenantSlug:string}>();const{user}=useTenantUser(tenantSlug);const[current,setCurrent]=useState("");const[next,setNext]=useState("");const[confirm,setConfirm]=useState("");const[message,setMessage]=useState("");const[error,setError]=useState("");if(!user)return <main className="p-8">Loading profile…</main>;return <TenantShell slug={tenantSlug} user={user} title="Profile"><div className="grid max-w-4xl gap-6 lg:grid-cols-2"><Card><CardHeader><h2 className="text-lg font-semibold">Identity</h2></CardHeader><CardContent className="space-y-2 text-sm"><p><strong>{user.full_name}</strong></p><p>{user.email}</p>{user.username&&<p>Username: {user.username}</p>}<p>Auth source: {user.auth_source.toUpperCase()}</p><p>Role: {user.role}</p><p>Status: {user.is_active?"Active":"Inactive"}</p><p>Last login: {user.last_login_at?new Date(user.last_login_at).toLocaleString():"Never"}</p></CardContent></Card><Card id="change-password"><CardHeader><h2 className="text-lg font-semibold">Password</h2></CardHeader><CardContent>{user.auth_source==="sso"?<p className="text-sm text-slate-500">Password is managed by your organization&apos;s identity provider.</p>:<form className="space-y-4" onSubmit={async e=>{e.preventDefault();setError("");setMessage("");if(next!==confirm){setError("Passwords do not match.");return}try{await tenantApi.changePassword(tenantSlug,{current_password:current,new_password:next});setMessage("Password changed successfully.");setCurrent("");setNext("");setConfirm("")}catch(err){setError(err instanceof Error?err.message:"Password change failed.")}}}>{error&&<Alert>{error}</Alert>}{message&&<Alert tone="success">{message}</Alert>}<FormField label="Current password" htmlFor="current"><Input id="current" type="password" value={current} onChange={e=>setCurrent(e.target.value)}/></FormField><FormField label="New password" htmlFor="new"><Input id="new" type="password" minLength={12} value={next} onChange={e=>setNext(e.target.value)}/></FormField><FormField label="Confirm password" htmlFor="confirm"><Input id="confirm" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></FormField><Button>Change password</Button></form>}</CardContent></Card></div></TenantShell>}
