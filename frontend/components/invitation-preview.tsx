import { CopyButton } from "@/components/copy-button";
import { Button } from "@/components/ui/button";
export function invitationText({ fullName, displayName, tenantUrl, setupLink }: { fullName: string; displayName: string; tenantUrl: string; setupLink: string }) { return `Hello ${fullName},\n\nYour PEKA tenant has been created.\n\nTenant:\n${displayName}\n\nTenant URL:\n${tenantUrl}\n\nAdministrator setup link:\n${setupLink}\n\nThis setup link expires in 24 hours and can only be used once.\n\nAfter setting your password, sign in locally and configure your organization’s SSO settings.\n\nRegards,\nPEKA Platform Administration`; }
export function InvitationActions({ email, fullName, displayName, tenantUrl, setupLink }: { email: string; fullName: string; displayName: string; tenantUrl: string; setupLink: string }) { const text = invitationText({ fullName, displayName, tenantUrl, setupLink }); const mailto = `mailto:${encodeURIComponent(email)}?subject=${encodeURIComponent(`Your PEKA tenant ${displayName} is ready`)}&body=${encodeURIComponent(text)}`; return <div className="flex flex-wrap gap-2"><CopyButton value={text} label="Copy full invitation"/><Button asChild><a href={mailto}>Open email app</a></Button></div>; }
