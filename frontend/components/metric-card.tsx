import { Card, CardContent, CardHeader } from "@/components/ui/card";
export function MetricCard({ label, value, detail }: { label: string; value: string | number; detail?: string }) { return <Card><CardHeader><p className="text-sm text-peka-secondary">{label}</p></CardHeader><CardContent><p className="text-2xl font-semibold">{value}</p>{detail && <p className="mt-1 text-xs text-peka-muted">{detail}</p>}</CardContent></Card>; }
