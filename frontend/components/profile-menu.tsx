"use client";
import Link from "next/link";import { useState } from "react";import { Button } from "@/components/ui/button";
export function ProfileMenu({label,profileHref,passwordManaged,onLogout}:{label:string;profileHref:string;passwordManaged?:boolean;onLogout:()=>void}){const[open,setOpen]=useState(false);return <div className="relative"><Button variant="ghost" aria-haspopup="menu" aria-expanded={open} onClick={()=>setOpen(!open)}>{label}</Button>{open&&<div role="menu" className="absolute right-0 mt-2 w-56 rounded-md border bg-white p-1 shadow-lg"><Link role="menuitem" className="block rounded px-3 py-2 text-sm hover:bg-slate-100" href={profileHref}>Profile</Link>{passwordManaged?<div className="px-3 py-2 text-xs text-slate-500">Password managed by SSO</div>:<Link role="menuitem" className="block rounded px-3 py-2 text-sm hover:bg-slate-100" href={`${profileHref}#change-password`}>Change password</Link>}<button role="menuitem" className="w-full rounded px-3 py-2 text-left text-sm hover:bg-slate-100" onClick={onLogout}>Log out</button></div>}</div>}
