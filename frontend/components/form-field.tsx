import type { ReactNode } from "react";
export function FormField({ label, htmlFor, error, hint, children }: { label: string; htmlFor: string; error?: string; hint?: string; children: ReactNode }) { return <div className="space-y-1.5"><label htmlFor={htmlFor} className="block text-sm font-medium text-peka-secondary">{label}</label>{children}{hint && <p className="text-xs text-peka-muted">{hint}</p>}{error && <p role="alert" className="text-sm text-peka-danger">{error}</p>}</div>; }
